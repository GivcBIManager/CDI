# CDI KB - local credentials. Gitignored; NEVER commit this file.
# Used by the LLM inference stage (cdi_kb.llm_infer) and live tests (pytest -m live).
# Add your key after the = sign, no quotes needed:
ANTHROPIC_API_KEY=
